<li>
  {{#link-to 'post' post.id}}
    <h3>{{post.title}}</h3>
  {{/link-to}}
  <p><cite>{{post.author}}</cite></p>
  <img src="{{post.image}}" class="post-tile-image" alt="{{post.title}}" />
  <p>{{post.teaser}}</p>
</li>
